# Development State

Updated 2026-08-23 (confirmed evidence only): the project is assembling a fixed-scene assistive-robot pipeline comprising ACT grasping, Gemini RGB-D mapping/localization, and supervised base navigation. The ACT v2 dataset/checkpoint and physical pick–short-transfer–place demonstrations exist, but grasp success is not yet repeatable because the policy has no explicit contact/success observation. The current SLAM map is `/data/slam/mapping/20260814T140025Z/rtabmap.db`, used only with the downward Gemini reference ID7 raw=4066/ID8 raw=1924; camera-only localization and Nav2 planning have passed, but RGB-D pose is not yet a navigation-grade high-rate controller. On branch `codex/fix-base-stop-idle-jitter`, the base executor now uses measured wheel feedback from IDs 7/8/9 for its 5 Hz local control pose, while RGB-D remains a liveness and translation-disagreement guard; it preserves ordered planner waypoints and steers to a 0.10 m lookahead point. Repeated read-only bus-cadence and raised-wheel zero-stop checks passed, and a grounded 0.04 m/s one-second pulse moved about 5 cm with a verified final zero/torque-off transaction. A 0.214 m supervised navigation run reached the configured 7 cm/8 degree tolerance and stopped safely; the latest 0.598 m run followed the intended right-turn/forward path for roughly 0.33 m but stopped safely when wheel and RGB-D translation differed by 0.379 m, exceeding the 0.200 m guard. The core files are `tools/nav2_supervised_base_execute.py`, `tools/base_stop_diagnostic.py`, `tools/base_shutdown_sequence_diagnostic.py`, their `scripts/jetson_slam_*` wrappers, and the related tests; the Jetson container passed 23 relevant unit tests at commit `e5275a6`. Do not relax the visual-disagreement guard or increase speed/path limits next: first inspect the saved execution traces to determine whether the visual measurement was an outlier or genuine drift, then implement and validate a temporally robust fusion/guard before re-running the same long route; retain the independent serial and verified-stop gates throughout.
