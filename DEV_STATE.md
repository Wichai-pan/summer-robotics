# Development State

Updated 2026-08-26 (confirmed evidence only): the project has a supervised fixed-scene MVP chain—Gemini RGB-D map/relocalization and Nav2 table docking followed by white-arm ACT fixed-object pick/place. The optional `scripts/jetson_nav_then_act_pick_place.sh` keeps the standalone tools and their `PIPELINE`/`RETURN`/`PLAN`/`MOVE`/`READY` gates; a failed child stage stops the chain. It defaults to manual-push map `/data/slam/mapping/20260825T131710Z/rtabmap.db`, table goal `(0.052, -0.357, -90 degrees)`, mapping and ACT gimbal references, and the 28-epoch checkpoint `/data/models/act_fixed_pick_place_v2_28ep_616995_006000`. The 2026-08-25 milestone is one operator-observed end-to-end baseline: table arrival was good, while grasping remained unstable. The latest traceable ACT log `/home/jetsonl7/robot-data/logs/20260825_204213_table_pick_place_01.log` ran 600 steps and completed rollout/final arm recovery, but was labelled `PARTIAL` because the gripper opened too late. Navigation retains verified active-brake/torque-off behavior, but wheel ID9 and the white-board bus can intermittently return `communication=-6`; the failed pipeline attempt `/data/slam/nav2-supervised-execute/20260825T173237Z/` stopped safely before motion. The older camera-only rotation disagreement and unverified wheel/IMU fusion remain relevant constraints, not reasons to weaken safety guards. Current `main` also contains the separate calibrated real-pick workflow in `sim_to_real/`; it has not been substituted for the fixed-scene ACT baseline. Next: run cadence and stop checks before each integrated trial, retain paired navigation and ACT artifacts, improve ACT timing/object alignment, and only then consider LM/VLA task-level decision integration. See `docs/slam/12-nav2-rotation-guard-and-base-reliability-20260822.md`, `docs/slam/16-nav-to-act-mvp-baseline-20260825.md`, and `docs/14-laboratory-robot-workflow-scenario-design.md`.
