# Development State

更新于 2026-08-15（仅记录已确认状态）：项目当前目标是把固定场景的 ACT 抓取与 Gemini RGB-D 的建图、重定位和受监督底盘导航接成一条可复现实机链路；ACT 方面，Jetson 上已有 28-episode / 19,309-frame 数据集与 v2 checkpoint，现场曾完成抓起、短距离左移和放下，但尚无稳定重复成功率，夹爪的负载/电流/位置判定还没有接入执行器；SLAM 方面，固定低头约 20° 的 Gemini 候选地图 `/data/slam/mapping/20260814T140025Z/rtabmap.db` 已在 9.10 m 手动闭环路线通过（位置闭环 5.4 cm、朝向闭环 0.71°），其云台参考为黑板 ID7 raw=4066、ID8 raw=1924，必须使用 `configs/slam/base_to_gemini_mapping_down_20deg_candidate.yaml` 对应的固定外参，不能在建图/定位期间移动云台；只读重定位、地图叠加图和 Nav2 `ComputePathToPose` dry-run 已通过，相关入口为 `scripts/jetson_slam_localization.sh`、`scripts/jetson_slam_nav2_planning_dry_run.sh`、`tools/render_localization_overlay.py`、`tools/make_nav2_goal_picker.py` 和 `tools/nav2_compute_path_dry_run.py`，planner 使用实时 `map -> odom -> base_link` 起点且不会驱动底盘；受监督执行入口 `scripts/jetson_slam_nav2_supervised_execute.sh` 与 `tools/nav2_supervised_base_execute.py` 只允许白板轮 ID7/8/9，保留二次 `MOVE` 确认、0.04 m/s、12 deg/s、路线/累计行程限制和停机回读，短距离约 10 cm 曾以停机回读确认成功，但长距离尚未验证：最新 0.688 m 计划在约 5 秒后全部轮子同时报 `communication=-6`，退出时 ID7/8 的扭矩与速度停机未能确认，说明问题位于底盘共同供电/总线/串口链路而非 Nav2 规划，事后静态扫描恢复不构成安全通过；此前把轮速误按 `/50` 换算会导致异常慢速，已确定应沿用 `tools/base_keyboard.py` 的既有原始轮速映射。当前工作分支为 `codex/nav2-planning-dryrun`，定位/Nav2/诊断文件及 `configs/nav2/` 仍是未提交改动；本机已做这些 Python 文件的 `py_compile` 与相关 shell 的 `bash -n`，但 `pytest` 未安装，且没有 Nav2 执行器单元测试。下一步应先在切断 12 V 后检查并压紧白板 12 V、白板→轮7、轮7→8、轮8→9 的供电/总线线缆，反复只读扫描确认 IDs 7/8/9 稳定，再做“带载通信 + 零速 + 三轮扭矩回读”可靠性门槛；在该门槛通过前不得继续长距离 Nav2，之后再修正中途转向/进度控制并复测同一条已规划路线，最后才考虑更长路径、轮编码器或 IMU 融合。
