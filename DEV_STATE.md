# Development State

Updated 2026-08-25 (confirmed evidence only): the project now has a supervised fixed-scene MVP baseline that chains Gemini RGB-D localization/Nav2 table docking with the existing ACT fixed-object pick/place rollout. The new optional host wrapper `scripts/jetson_nav_then_act_pick_place.sh` preserves all standalone diagnostic scripts and their `PIPELINE`/`RETURN`/`PLAN`/`MOVE`/`READY` confirmations; a child-stage failure stops the chain before the next stage. It defaults to the current manual-push map `/data/slam/mapping/20260825T131710Z/rtabmap.db`, table goal `(0.052, -0.357, -90 degrees)`, mapping gimbal reference `/data/config/gemini_gimbal_mapping_down_20deg_v1.json`, ACT gimbal reference `/data/config/gemini_gimbal_grasp_pose_v1.json`, and the newer 28-epoch checkpoint `/data/models/act_fixed_pick_place_v2_28ep_616995_006000`. Jetson has the wrapper at commit `53a14b9` and its no-hardware help check passed. A pipeline localization/planning attempt saved under `/data/slam/nav2-supervised-execute/20260825T173237Z/`: it localized near `(0.102, 0.039, -93.8 degrees)` and planned 0.449 m to the table goal, then failed before motion when wheel ID9 torque enable returned `communication=-6`; the active brake and final three-wheel zero/torque-off readback passed, and ACT was not launched. The latest traceable ACT experiment is `/home/jetsonl7/robot-data/logs/20260825_204213_table_pick_place_01.log`: 600 steps with the 28-epoch checkpoint, rollout and final arm recovery passed, but the operator labelled it `PARTIAL` because the gripper opened too late. The operator also reports one end-to-end attempt reached the table area well but with unstable grasping; this is a baseline observation, not a measured success rate. Read-only cadence and zero-velocity shutdown checks have passed on a healthy power/serial session, but intermittent white-board communication remains an operational risk. Core code is `tools/nav2_supervised_base_execute.py`, `scripts/jetson_nav_then_act_pick_place.sh`, `scripts/jetson_slam_nav2_supervised_execute.sh`, and `scripts/jetson_act_trial.sh`; the full milestone record is `docs/slam/16-nav-to-act-mvp-baseline-20260825.md`. Next: run cadence and shutdown checks before every integrated trial, pair each unique pipeline label with its navigation directory and ACT log, then improve ACT object/closure timing without changing the validated navigation safety gates.
