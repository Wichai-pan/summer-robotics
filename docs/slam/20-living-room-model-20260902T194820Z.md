# 客厅房间建模结果（2026-09-02）

## 最终结果

本项目最近一次通过复核的房间建模会话为 `20260902T194820Z`。机器人使用头部
Orbbec Gemini 335（固定向下约 20°）采集 RGB-D 数据，并由 RTAB-Map 建立客厅地图。

![客厅二维占据地图](../../artifacts/slam/20260902T194820Z/occupancy-map/living-room-20260902T194820Z.png)

可直接复用的导航地图文件：

- [PNG 预览](../../artifacts/slam/20260902T194820Z/occupancy-map/living-room-20260902T194820Z.png)
- [PGM 占据栅格](../../artifacts/slam/20260902T194820Z/occupancy-map/living-room-20260902T194820Z.pgm)
- [ROS map_server 配置](../../artifacts/slam/20260902T194820Z/occupancy-map/living-room-20260902T194820Z.yaml)

## 建模质量

| 项目 | 结果 |
| --- | --- |
| RTAB-Map 会话 | 1 |
| 保存节点 | 575 |
| 优化位姿 | 124 |
| 相邻约束 | 123 |
| 全局 / 局部空间闭环 | 21 / 12 |
| RGB、深度、地面和障碍栅格 | 575 个节点全部保留 |
| 二维地图 | 112 × 128，0.05 m/格 |
| 地图原点 | `[-2.67605, -4.75647, 0]` |
| 建图过程跟踪丢失 | 0 次 |

建图报告的最大帧间隔为 `0.500594 s`，比原定 `0.500 s` 门槛高约 0.6 ms，因此没有只凭
地图外观宣布成功。随后使用同一数据库进行了 90 秒静止定位复测：平均 `10.055 Hz`、
0 次跟踪丢失、最大帧间隔 `0.367 s`、首尾净漂移 `2.3 mm`，复测通过。

数据库 SHA-256：
`b08da431075379428938069a50d87e2b63bc61cf9e3ff8a78199c092a9a50655`。
SQLite 完整性和外键检查均通过。

## 后续验证

- Nav2 planner-only 在约 `1.4 ms` 内成功生成 7 点短路径，未连接底盘控制器。
- 使用 0.30 m 圆形 footprint 审计时，起点可通行；与起点连通的保守自由面积约
  `2.775 m²`，地图坐标边界约为 `x=[-1.626, 1.574] m`、
  `y=[-2.856, 0.594] m`。
- 监督式实机验证完成旋转、前进、左右横移及返回；最终独立定位距初始基准约 3.2 cm，
  数据库、TF、定位和当前位置 footprint 联合健康检查通过。

更详细的导航复核、失败项和家具作业区标定仍保留在现场实验记录中；这些内容将在相关脚本、
配置和证据整理完成后单独归档，避免本次地图发布引入不完整入口。

## 文件边界

Git 仓库只保存便于查看与复用的二维地图。原始 RTAB-Map 数据库、运行日志和 3D 点云留在
实验数据存储中，避免把大文件直接提交到普通 Git 历史。二维地图可由 ROS 2
`nav2_map_server` 读取配套 YAML；使用前仍须完成现场清障、最新定位和 footprint 检查。
