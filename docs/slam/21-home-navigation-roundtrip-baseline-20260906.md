# 家庭导航首个稳定往返基线（2026-09-06）

## 基线结论

2026-09-06 完成桌面工作区与沙发工作区之间的首次稳定监督式往返。正向与反向路线均由
5 个受保护路段组成，全部路段通过；两次到达均进入工作位 `0.03 m / 2°` 验收门限。

本次结果标记为导航代码的首个稳定成功基线：`home-nav-roundtrip-20260906`。

## 地图身份

```yaml
map: 20260902T194820Z
sha256: b08da431075379428938069a50d87e2b63bc61cf9e3ff8a78199c092a9a50655
```

Git 仓库不保存约 228 MB 的 RTAB-Map 数据库。运行时必须使用上述时间戳与 SHA-256 对应的
只读地图数据库工作副本，不得混入其他建图会话。

## 四个已记录定位点

| 定位点 | 配置 ID | 地图位姿 `(x, y, yaw)` |
| --- | --- | --- |
| 桌面工作位 | `right_a.work_pose` | `(0.229, -0.590, 1.716°)` |
| 桌前准备位 | `right_a.predock_pose` | `(-0.032, -0.598, -0.115°)` |
| 沙发前准备位 | `right_c.predock_pose` | `(0.044, -1.482, -0.689°)` |
| 沙发工作位 | `right_c.work_pose` | `(0.229, -1.505, 3.097°)` |

这些点固定在
[`configs/nav2/home_workspaces_20260902T194820Z.yaml`](../../configs/nav2/home_workspaces_20260902T194820Z.yaml)。
工作位与二维占据图中的家具可能重叠，只能使用已验证的精确靠站模式；不能把工作位当作普通
自由空间目标，也不能据此放宽其他区域的 footprint 检查。

## 往返结果

| 方向 | 运行时间戳 | 路段结果 | 最终位置误差 | 最终航向误差 |
| --- | --- | ---: | ---: | ---: |
| 桌面 → 沙发 | `20260906T160500Z` | 5/5 `PASS` | `0.0232 m` | `1.86°` |
| 沙发 → 桌面 | `20260906T161007Z` | 5/5 `PASS` | `0.0297 m` | `1.82°` |

两个方向均完成：

- 每个路段独立规划并在受限速度、路径长度、累计行程和运行时间内执行；
- 跨工作区采用全向路径跟随，工作位进出采用先对准目标航向再全向平移的靠站模式；
- 路线指定位置进行静止重定位和最终工作位修正；
- 完成主动制动、关闭三只轮子扭矩，并通过停车回读确认。

精简报告只保留成功判据与运行时间戳。完整原始运行目录、相机数据及其他现场日志不进入 Git。

## 固定入口

```bash
# 桌面工作位 -> 沙发工作位
./scripts/jetson_home_navigate_table_to_sofa_continuous.sh

# 沙发工作位 -> 桌面工作位
./scripts/jetson_home_navigate_sofa_to_table_continuous.sh
```

两条路线分别由以下配置定义：

- [`table_to_sofa_continuous_20260902T194820Z.yaml`](../../configs/nav2/table_to_sofa_continuous_20260902T194820Z.yaml)
- [`sofa_to_table_continuous_20260902T194820Z.yaml`](../../configs/nav2/sofa_to_table_continuous_20260902T194820Z.yaml)

## 适用边界

这是固定地图、固定四点和现场监督条件下的成功基线，不代表未知房间自主导航，也不代表动态
障碍停车已经验收。再次运行前仍须保持双臂收回、确认整条路线净空、验证 Gemini 固定姿态、
完成起点连续性检查，并由现场人员持有 12 V 急停能力。任何定位、TF、路径、安全门或停车回读
失败都必须终止整条路线。
