# LeRobotDataset runtime for Jetson/aarch64.
#
# Do not install the generic ``lerobot[dataset]`` extra here: on aarch64 it
# currently selects torchcodec>=0.11, which requires a newer generic PyTorch
# and would replace the NVIDIA Jetson torch build.  PyAV is LeRobot's supported
# fallback video backend and works with the NGC image's FFmpeg libraries.
datasets>=4.7.0,<5.0.0
pandas>=2.0.0,<3.0.0
pyarrow>=21.0.0,<30.0.0
av>=15.0.0,<16.0.0
jsonlines>=4.0.0,<5.0.0
